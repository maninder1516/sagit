{% extends 'baseizmo.html.twig' %}

{% block title %}Missions!{% endblock %}

{% block content %}
<!-- Main content -->
<div class="container-xl">
     <div class="table-responsive">
        <div class="table-wrapper">
            <div class="table-title">
				<div class="row">
					<div class="col-sm-6">
						<h2>Manage <b>Missions</b></h2>
					</div>
					<div class="col-sm-6">
                        {# <a href="{{ path('izmo_students_create') }}" class="btn btn-success"><i class="material-icons">&#xE147;</i><span>Add New Student</span></a> #}
					</div>
				</div>
			</div>
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>
                            <span class="custom-checkbox">
                                <input type="checkbox" id="selectAll">
                                <label for="selectAll"></label>
                            </span>
                        </th>
                        <th>Username</th>
                        <th>Service Date</th>
                        <th>Product Name</th>						 
                        <th>Vendor Name</th>						                         					 
                        <th class="th-action">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {# {% for student in students %}
                        <tr>
                            <td>
                                <span class="custom-checkbox">
                                    <input type="checkbox" id="checkbox1" name="options[]" value="1">
                                    <label for="checkbox1"></label>
                                </span>
                            </td>
                            <td>{{ student.name }}</td>
                            <td>{{ student.age }}</td>
                            <td>{{ student.email }}</td>
                            <td>
                                <a href=/student/view/{{student.id}} class="view"><i class="material-icons" title="View">&#xE417;</i></a>
                                <a href=/student/edit/{{student.id}} class="edit"><i class="material-icons" title="Edit">&#xE254;</i></a>
                                <a href=/student/delete/{{student.id}} class="delete" onclick="return confirm('Are u sure?')"><i class="material-icons" title="Delete">&#xE872;</i></a>                               
                            </td>
                        </tr>
                    {% endfor %} #}
                </tbody>                
            </table> 
            <div class="navigation font-weight-bold">
            {# <strong> {{ knp_pagination_render(missions) }} </strong> #}
            </div>           
        </div>
    </div>
</div>

<section class="content">
	<div class="row">
        <div class="col-xs-12">
        	<div class="box">
        		<!-- / box-header -->
        		<div class="box-header">
	              <h3 class="box-title">Missions</h3>
	              {% if 1 %}
	              {# <a class="btn btn-info pull-right" title="Add mission" alt="Add mission" href="{{ path('aqf_addedit', {'id': 0}) }}"><i class="fa fa-plus" aria-hidden="true"></i> Add New</a> #}
	              {% endif %}
	            </div>
	            {# <div class="box-header pull-right">
	            	Search: 
	            	<input type="text" id="txtSearch" name="txtSearch" value ="{{searchText}}" onkeydown="doSearchOnEnterkeyPress(event)" />
        			<input type="button" class="button-primary" value="Search" onclick="doSearch()" />
            	</div> #}
	            <!-- /.box-header -->
	            <div class="box-body">
	            	<table id="tbl_missions" class="table table-bordered table-striped">
	            		<thead>
			                <tr>
			                  {# {% if role == 1 %} #}
			                  <th>Username</th>
			                  {# {% endif %} #}
			                  <th>Service Date</th>
			                  <th>Product Name</th>
			                  <th>Vendor Name</th>
			                  <th>Actions</th>
			                </tr>
		                </thead>
		                {# <tbody>
	                		{% for mission in missions %}
			                <tr>
			                  {% if role == 1 %}
			                  <td> {{ mission.username }}</td>
			                  {% endif %}
			                  <td><a href="{{ url('aqf_view',{'id': mission.id}) }}">
			                  	{{ mission.serviceDate|date('d-m-Y') }} </a>
			                  </td>
			                  <td><a href="{{ url('aqf_view',{'id': mission.id}) }}">
			                  	 {{ mission.productName }} </a>
			                  </td>
			                  <td> {{ mission.vendorName }}</td>
			                  <td>
			                  	{% if role == 2 %}
			                  	<a href="{{ url('aqf_addedit',{'id': mission.id}) }}"><i class="fa fa-edit"></i></a>&nbsp;&nbsp;&nbsp;
			                  	{% endif %}
			                  	<a href="{{ path('aqf_view',{'id': mission.id}) }}"><i class="fa fa-eye"></i></a>&nbsp;&nbsp;&nbsp;
			                  	<a href="{{ url('aqf_delete',{'id': mission.id}) }}"><i class="fa fa-trash-o" style="color:red"></i></a>
		                  	  </td>
			                </tr>
			            	{% endfor %}
			            </tbody> #}
	            	</table>
	            	<div class="navigation font-weight-bold">
				    {# <strong> {{ knp_pagination_render(missions) }} </strong> #}
					</div>
	            </div>
	            
        	</div>
        	<!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>
<!-- /.Main content -->
{% endblock %}
